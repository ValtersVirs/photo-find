<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PhotoFind" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="colorPrimaryVariant">@color/orange_dark</item>
        <item name="colorOnPrimary">@color/puce</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/orange</item>
        <item name="colorSecondaryVariant">@color/orange_dark</item>
        <item name="colorOnSecondary">@color/puce</item>

        <!-- Custom. -->
        <item name="toolbarStyle">@style/Widget.App.Toolbar</item>
        <item name="actionMenuTextColor">@color/olive_green</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:textColor">@color/puce</item>
        <item name="android:textColorPrimary">@color/puce</item>
        <item name="android:colorBackground">@color/beige</item>
        <item name="materialButtonStyle">@style/Widget.App.Button</item>
        <item name="editTextStyle">@style/Widget.App.EditText</item>
        <item name="tabStyle">@style/Widget.App.TabLayout</item>
        <item name="switchStyle">@style/Widget.App.Switch</item>
        <item name="materialAlertDialogTheme">@style/ThemeOverlay.App.MaterialAlertDialog</item>
    </style>
</resources>